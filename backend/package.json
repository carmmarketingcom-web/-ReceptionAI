{
  "name": "receptionai-backend",
  "version": "0.1.0",
  "private": true,
  "description": "ReceptionAI — Multi-tenant AI receptionist SaaS backend",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun run db/seed.ts",
    "db:drop": "drizzle-kit drop"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2",
    "typescript": "^5.5.0"
  }
}
