{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && rm -rf dist && bun run build"
  },
  "deploy": {
    "startCommand": "bun run serve.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
